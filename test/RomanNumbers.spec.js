const { expect } = require('chai');
const { convertDigits } = require('../src/RomanNumbers');

describe('Roman Numerals', () => {
	describe('convertDigits()', () => {
		it('dummy function', () => {
			expect(convertDigits()).to.equal('hello');
		});
	});
});
